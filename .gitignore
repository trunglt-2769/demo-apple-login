# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
/build

# storybook production
/storybook-static

# misc
.DS_Store
.stylelintcache
.eslintcache

# environment variable files
.env
.env.production
.env.local
.env.*.local

# dependency lock files
# we only use Yarn as our official Node package manager, and so we will only
# commit yarn.lock file, and ignore package-lock.json file
package-lock.json

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
